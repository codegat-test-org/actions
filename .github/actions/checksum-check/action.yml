name: Checksum Check
inputs:
  checksum-location:
    type: string
    required: true
    description: Location of the checksums that will be compared against
  checked-against:
    type: string
    required: false
    description: Explicit config tag to checkout the 'ground truth' checksum. Defaults to last major truth update.
outputs:
  result:
    value: ${{ steps.check.outputs.result }}
    description: The result of the comparison between the artifact and the latest ground truth
  ground-truth-version:
    value: ${{ steps.ground-truth.outputs.version }}
    description: The ground truth version compared against the given artifact
runs:
  using: composite
  steps:
    - name: Determine Tag to Check Against
      shell: bash
      id: ground-truth
      run: |
        if [[ "${{ inputs.config-tag }}" == "" ]]; then
          echo "version=$(git describe --tags --abbrev=0)" >> $GITHUB_OUTPUT
        else
          echo "version=${{ inputs.config-tag }}" >> $GITHUB_OUTPUT
        fi

    - name: Compare Checksum against ${{ steps.ground-truth.outputs.version }}
      shell: bash {0}
      id: check
      run: |
        git checkout ${{ steps.ground-truth.outputs.version }}
        diff ${{ inputs.checksum-location }}/CHECKSUM CHECKSUM
        if [ "$?" -eq 0 ]; then
          echo "result=true" >> $GITHUB_OUTPUT
        else
          echo "result=false" >> $GITHUB_OUTPUT
        fi